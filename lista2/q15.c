// Importar as bibliotecas
#include <stdio.h>
#include <math.h>

int main () {
    // Estrutura de repeticao
    for (int i = 0; i <= 7; i++) {
        printf("3^(%d) = %.2f\n", i, pow(3, i));

    }


    return 0;
}